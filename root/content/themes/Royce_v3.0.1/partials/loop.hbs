{{!-- The post feed with featured posts excluded --}}
{{#get "posts" include="tags,tiers" limit=@config.posts_per_page filter="featured:false" page=pagination.page}}
    {{#foreach posts}}
        {{> "loop-item"}}
    {{/foreach}}
    {{!-- Previous/next page links --}}
    {{pagination}}
{{/get}}
